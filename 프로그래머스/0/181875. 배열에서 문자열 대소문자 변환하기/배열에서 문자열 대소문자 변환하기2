class Solution {
    public String[] solution(String[] strArr) {

        //삼항 연산자 사용, 짝수면 소문자로 홀수면 대문자로 출력
        for(int i = 0; i < strArr.length; i++){
            strArr[i] = i % 2 == 0 ? strArr[i].toLowerCase() : strArr[i].toUpperCase();
        }
        return strArr;
    }
}
